# Neon: copie a connection string em Neon Console > Project > Connect
# Use o endpoint "pooled" (host com -pooler) para a aplicacao.
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require

# Obrigatorio em producao (gere com: python -c "import secrets;print(secrets.token_hex(32))")
SECRET_KEY=

SUPERADMIN_USER=superadmin
SUPERADMIN_PASS=
